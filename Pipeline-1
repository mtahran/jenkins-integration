pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
        stage('passwd') {
            steps {
                sh 'cat /etc/passwd'
            }
        }
    }
}